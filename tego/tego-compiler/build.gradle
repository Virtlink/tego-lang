plugins {
    id 'java'
    id 'org.jetbrains.kotlin.jvm'
}

dependencies {
    // Code Generation
    implementation "com.virtlink:kasm:0.1-SNAPSHOT"

    // Runtime
    implementation project(":tego-runtime")
}

tasks.withType(Test.class) {
    //debug true
    maxHeapSize "3g"
}