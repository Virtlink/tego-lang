package org.spoofax.tego.compiler

import org.spoofax.tego.aterm.ApplTerm
import org.spoofax.tego.aterm.Term
import org.spoofax.tego.ir.Exp
import org.spoofax.tego.ir.IrBuilder
import org.spoofax.tego.ir.Var

/**
 * Compiles a Tego strategy to a class.
 */
class TegoCompiler {

    fun compileStrategy() {
        // Takes an expression
        // 1. Convert to IR
        // 2. Change to ANF
        // 3. Write out to class
    }

    /**
     * Compiles an expression.
     */
    fun compileExpression(exp: Exp) {

    }
}